import { Telegraf } from "telegraf"; // Importing the Telegraf module
import type { Context } from "../types";

const registerKeyboard = (bot: Telegraf<Context>) => {};

export default registerKeyboard;
